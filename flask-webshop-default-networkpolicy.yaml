apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: flask-webshop-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/flask-webshop-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/flask-webshop-default: "true"
